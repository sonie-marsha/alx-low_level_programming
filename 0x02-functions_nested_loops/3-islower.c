#include "main.h"

/**
 *_islower - checks for lowercase char
 *@c: argument
 *
 *Return: (1) if lowercase
 *o/w (0)
 */

int _islower(int c)
{
	if (c = 97 && c <= 122)
		return (1);
	else
		return (0);
}
