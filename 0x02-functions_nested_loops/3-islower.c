#include "main.h"

/**
 *_islower - checks for lowercase char
 *
 *Return: (1) if lowercase
 *o/w (0)
 */

int _islower(int c)
{
	int c;

	if (c = 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
